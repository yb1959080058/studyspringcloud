server:
  port: 9901

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/


spring:
  #  datasource:
  #    url: jdbc:mysql://192.168.50.63:3306/rx?allowMultiQueries=true&useUnicode=true&characterEncoding=UTF-8
  #    username: root
  #    password: yxt20180303
  application:
    name: service-eureka-server

