server:
  port: 9904
spring:
  application:
    name: service-config-client
  cloud:
    config:
      label: master
      profile: dev
      uri: http://localhost:9903/
management:
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        include: health, info, refresh