server:
  port: 9903

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9901/eureka/

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yb1959080058/uaps.git
          searchPaths: configFiles
  application:
    name: service-config-server